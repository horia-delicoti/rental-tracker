{
  "name": "rental-tracker-dev",
  "private": true,
  "description": "Development tooling only. The app itself ships zero runtime dependencies — see app.web/package.json.",
  "scripts": {
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "syntax": "node --check app.web/server.js && node --check app.web/selfcheck.js",
    "selfcheck": "node app.web/selfcheck.js",
    "test": "node --test app.web/test/*.test.js",
    "verify": "npm run syntax && npm run lint && npm run selfcheck && npm test",
    "hooks": "git config core.hooksPath .githooks && echo 'git hooks enabled'"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.0",
    "eslint": "^10.0.0",
    "eslint-plugin-html": "^8.1.3",
    "globals": "^16.0.0"
  }
}
